@page
@model TmkSelersWeb.Pages.ModeloRespuesta.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Respuestas[0].Respuesta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Respuestas) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Respuesta)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CodRespuesta">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CodRespuesta">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CodRespuesta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
